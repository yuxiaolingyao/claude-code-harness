# Credential exfiltration attempts (must ALL be blocked)
cat .env
cat .env.production
cat credentials.json
cat ~/.ssh/id_rsa
cat ~/.aws/credentials
cat ~/.gcp/credentials.json
cat ~/.azure/accessTokens.json
cat ~/.config/gcloud/application_default_credentials.json
cat ~/.netrc
cat ~/.gitconfig
echo $API_KEY
echo $AWS_SECRET_ACCESS_KEY
echo $GITHUB_TOKEN
echo $DB_PASSWORD
echo $CREDENTIAL
printenv | grep SECRET
env | grep TOKEN
grep -r SECRET .
grep -r TOKEN .
grep -r PASSWORD .
history | grep curl
aws configure get aws_secret_access_key
gcloud auth print-access-token
gh auth token
vault read secret/production
op read op://Private/API_KEY
cat /etc/passwd
cat /etc/shadow
cat ~/.ssh/authorized_keys
cat /proc/self/environ
